<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Menus</title>
  </head>
  <body>
    <h1>Menus</h1>

      <% @menus.each do |menu| %>
        <h4><a href="/menus/<%= menu.id %>"><%= menu.name %></a></h4>
      <% end %>
  </body>
</html>
